# Abhängigkeiten des Desktop-Programms.
Flask>=3.0
pandas>=2.0
PyYAML>=6.0

# Lokaler, offline lernender Kategorisierungs-Klassifikator:
scikit-learn>=1.3
joblib>=1.3

# Optional – nur für den automatischen PSD2-Kontoabruf (GoCardless):
requests>=2.31

# pywebview (natives Fenster) wird von den Startskripten separat installiert.
# Schlägt das fehl, öffnet sich das Programm stattdessen im Browser.
#
# Die alte Streamlit-Oberfläche ist NICHT enthalten – sie zieht sehr tief
# verschachtelte Dateien nach sich und sprengt auf Windows schnell die
# 260-Zeichen-Pfadgrenze. Wer sie braucht:
#     pip install -r requirements-streamlit.txt
